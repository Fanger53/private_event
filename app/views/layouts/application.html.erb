<!DOCTYPE html>
<html>

<head>
  <title>PrivateEvent</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
        <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
      <nav class="navbar-expand-lg bg-dark d-flex justify-content-between mt-3">
        <div class="form-title text-white w-25">
          <span>private events</span>
        </div>
        <ul class="d-flex justify-content-between text-white w-50 list-unstyled">
          <% if current_user%>
            <li class="w-25">
              <%= link_to 'logout' , logout_path, method: :delete %>
            </li>
            <li class="w-25">
              <%= link_to 'create event' , new_event_path %>
            </li>
            <li class="w-25">
              <%= link_to 'events' , events_path %>
            </li>
            <li><%= link_to current_user.username, user_path(current_user) %></li>
            <%else%>
              <li class="w-25">
                <%= link_to 'login' , login_path %>
              </li>
              <li><%= link_to "Signup now!" , signup_path %>
              <li><%= link_to 'events', events_path %></li>
              <li><%= link_to 'create event', new_event_path %></li>
              </li>
              <%end%>

        </ul>
      </nav>
       <% flash.each do |name, message| %>
    <div class="<%= name %> text-center alert alert-info ">
      <%= message %>
    </div>
    <% end %>
      <%= yield %>
</body>

</html>